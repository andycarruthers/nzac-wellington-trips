baseURL = "https://trip-reports.wellingtonalpineclub.org.nz/"
languageCode = "en-nz"
title = "NZAC Wellington — Trip Reports"
enableRobotsTXT = true

[params]
  description = "Trip reports from the Wellington Section of the New Zealand Alpine Club"
  siteName = "NZAC Wellington"

[taxonomies]
  tag = "tags"
  location = "locations"
  author = "authors"

[markup.goldmark.renderer]
  unsafe = true

[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]
  taxonomy = ["HTML"]
  term = ["HTML"]
